/**
 * @description Utility type for `MultiSelectField` and `SingleSelectField` which represents the available field options.
 * @type SelectOptions
 * @category Utility
 */
export type SelectOptions = {
  name: string;
  color: string;
};
